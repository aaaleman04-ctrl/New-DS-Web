# Copia este archivo a `.env` o `.env.local` en la raíz del proyecto y reemplaza los valores.
# Obtén la URL y la anon key en: Supabase Dashboard → tu proyecto → Settings → API

NEXT_PUBLIC_SUPABASE_URL=https://tu-id-de-proyecto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu-anon-key-publica
